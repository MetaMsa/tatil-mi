name: Keep Railway Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Railway App
        run: |
          curl -L https://tatilmi.benserhat.com/